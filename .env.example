# Claude / Anthropic
ANTHROPIC_API_KEY=

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Server-only key — bypasses RLS so writes to scan_assignments/presence/audit/snapshots work.
# Find it in Supabase dashboard → Project Settings → API → service_role. NEVER expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Google OAuth (Drive API)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://YOUR-APP.railway.app/oauth/callback

# Reviewer emails (comma-separated, no spaces)
REVIEWER_EMAILS=marc@astreet.com,heather@astreet.com

# App password (HTTP Basic Auth — shared between Sam and Jamie)
APP_PASSWORD=changeme

PORT=3000
